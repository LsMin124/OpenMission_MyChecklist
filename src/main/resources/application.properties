spring.application.name=demo

# server config
server.port=8080
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true

# DB config (localhost for dev(docker), change when using aws)
spring.datasource.url=jdbc:postgresql://localhost:5432/todo_db
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA, Hibernate config

# DB dialect for postgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# DDL Auto - entity & DB schema matching ('create' for reset, 'update' for dev, 'validate' for real service)
spring.jpa.hibernate.ddl-auto=update

# SQL logging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Real value logging
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# JWT Configuration
jwt.secret=VGhpc0lzQVZlcnlTZWNyZXRLZXlGb3JNeVRvZG9MaXN0UHJvamVjdFRvTWFrZUl0U2VjdXJlQW5kU2FmZTIwMjQ=
# token expiration for 1 hour (msec)
jwt.expiration=3600000